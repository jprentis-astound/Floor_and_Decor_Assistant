[build]
builder = "nixpacks"
buildCommand = "cd roomy-assistant/packages/agent && pip install -r requirements.txt"

[deploy]
startCommand = "cd roomy-assistant/packages/agent && uvicorn server:app --host 0.0.0.0 --port ${PORT:-8080}"
healthcheckPath = "/health"
